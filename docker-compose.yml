version: '3.7'

services:
  nginx:
    build:
      context: ./nginx
      network: host
    image: shane-slashgif-site-nginx:${VERSION:-latest}
    env_file: settings.env
    deploy:
      mode: global
      resources:
        limits:
          cpus: '2.0'
          memory: 50M
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=traefik-public"
        - "traefik.constraint-label=traefik-public"
        - "traefik.http.routers.STACK_NAME-http.rule=Host(${TRAEFIK_HOST})"
        - "traefik.http.routers.STACK_NAME-http.entrypoints=http"
        - "traefik.http.routers.STACK_NAME-http.middlewares=https-redirect"
        - "traefik.http.routers.STACK_NAME-https.rule=Host(${TRAEFIK_HOST})"
        - "traefik.http.routers.STACK_NAME-https.entrypoints=https"
        - "traefik.http.routers.STACK_NAME-https.tls=true"
        - "traefik.http.services.STACK_NAME.loadbalancer.server.port=80"
        - "traefik.http.services.STACK_NAME.loadbalancer.server.scheme=http"
    volumes:
      - data_dir:/data
    depends_on:
      - app
    ports:
      - "80:80"

  app:
    build:
      context: ./app
      network: host
    image: shane-slashgif-site-app:${VERSION:-latest}
    env_file: settings.env
    command: "gunicorn slashgif_site.wsgi:application -w 2 -b 0.0.0.0:9000"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9000/app-health-check/"]
      interval: 1m
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      mode: global
      resources:
        limits:
          cpus: '2.0'
          memory: 200M
    volumes:
      - data_dir:/data

volumes:
  data_dir:
